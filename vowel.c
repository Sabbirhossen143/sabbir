#include<stdio.h>
int main()
{
  char a;
  printf("enter a word= ");
  scanf("%c",&a);
    if(a=='a' || a=='e' || a=='i' || a=='o' || a=='u' || a=='A' || a=='E' || a=='I' || a=='O' || a=='U')
        printf("it is a vowel");
    else
        printf("it is a consonent");
    return 0;











}
