#include<stdio.h>
int sort(int b)
{
    int sum=b[0];
    if(sum<b[i])
        {
        for(PTR=1;PTR<=n-k;){
        if(data[PTR]>data[PTR+1]){
          a=data[PTR];
          data[PTR]=data[PTR+1];
          data[PTR+1]=a;
          PTR++;
        }
        }
    return b[i];
}
int largest(int )
int main()
{
  int N,k
  scanf("%d",&N);
  for(i=0;i<N;i++)
    {
        scanf("%d",&a[i]);
    }
    sort(int a[i]);
}
